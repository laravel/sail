    meilisearch:
        image: 'getmeili/meilisearch:latest'
        ports:
            - '${FORWARD_MEILISEARCH_PORT:-7700}:7700'
        volumes:
            - '${FORWARD_VOLUMES:-sail}meilisearch:/data.ms'
        networks:
            - sail
