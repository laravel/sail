mailpit:
    image: 'axllent/mailpit:latest'
    ports:
        - '${SAIL_IP:-172.20.0.10}:${FORWARD_MAILPIT_PORT:-1025}:1025'
        - '${SAIL_IP:-172.20.0.10}:${FORWARD_MAILPIT_DASHBOARD_PORT:-8025}:8025'
    networks:
        - sail
