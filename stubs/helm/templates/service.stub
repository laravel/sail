apiVersion: v1
kind: Service
metadata:
  name: {{ include "sail.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "sail.name" . }}
    tier: web